# mimo-blog 3.0 前端重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从 release/2.0 切出 release/3.0，彻底删除并重建 `web/` 前端和 `nginx/`，保留后端与基础设施，使用 TanStack Start + Tailwind CSS v4 + shadcn/ui + ReactBits + Biome 重新实现博客全部功能。

**Architecture:** 基于 feature 的目录结构，TanStack Router 文件路由，TanStack Query 管理服务端状态，Zustand 管理客户端状态，React Hook Form + Zod 管理表单，SSR 首屏 + lazy Suspense 处理浏览器专属组件。

**Tech Stack:** React 19, TypeScript 6, Vite 7, TanStack Start, TanStack Router, TanStack Query, TanStack Form, Tailwind CSS v4, shadcn/ui, ReactBits, Biome, Vitest, Zustand, Sonner.

## Global Constraints

- 一个文件只允许写一个 React 组件。
- 单组件文件建议不超过 400 行，允许合理浮动，超过时拆分子组件到新文件。
- 表单验证统一使用 React Hook Form + Zod，所有校验逻辑收敛到 `features/<feature>/schemas/`，禁止手写 `if/else` 校验。
- 禁止原生 `confirm` / `alert` / `prompt`，统一使用 shadcn/ui `AlertDialog` 或 Sonner toast。
- 所有 `interface` / `type` 和 React props 属性必须写 JSDoc 风格注释。
- 状态/类型/角色等有限取值优先使用 `const` 对象 + `as const` 或 TypeScript enum。
- API 调用和 TanStack Query key 工厂集中在 `features/<feature>/api/`。
- 环境变量集中在 `lib/env.ts`，禁止业务代码直接使用 `import.meta.env`。
- 错误统一通过 Error Boundary 和 Sonner toast 展示。
- 每次提交遵循 Conventional Commits，保持小而原子。

---

## File Structure

```
web/
├── app/
│   ├── __root.tsx
│   ├── routeTree.gen.ts
│   └── styles.css
├── routes/
│   ├── _public.tsx
│   ├── _public.index.tsx
│   ├── _public.about.tsx
│   ├── _public.blog.index.tsx
│   ├── _public.blog.$slug.tsx
│   ├── _public.projects.index.tsx
│   ├── _public.music.tsx
│   ├── _public.profile.tsx
│   ├── _auth.tsx
│   ├── _auth.login.tsx
│   ├── _auth.register.tsx
│   ├── _auth.verify-email.tsx
│   ├── admin.tsx
│   └── admin/
│       ├── index.tsx
│       ├── posts.index.tsx
│       ├── posts.new.tsx
│       ├── posts.$id.edit.tsx
│       ├── users.tsx
│       ├── comments.tsx
│       ├── media.tsx
│       ├── settings.tsx
│       ├── roles.tsx
│       ├── announcements.tsx
│       ├── emojis.tsx
│       ├── playlists.tsx
│       └── logs.tsx
├── features/
│   ├── auth/
│   ├── posts/
│   ├── projects/
│   ├── music/
│   ├── profile/
│   ├── admin-dashboard/
│   ├── admin-posts/
│   ├── admin-users/
│   ├── admin-comments/
│   ├── admin-media/
│   ├── admin-settings/
│   ├── admin-roles/
│   ├── admin-announcements/
│   ├── admin-emojis/
│   ├── admin-playlists/
│   └── admin-logs/
├── components/
│   ├── ui/
│   ├── shared/
│   └── reactbits/
├── lib/
│   ├── api.ts
│   ├── env.ts
│   └── utils.ts
├── tests/
├── biome.json
├── package.json
├── tsconfig.json
├── vite.config.ts
└── app.config.ts
```

---

## Phase 1: 分支清理与项目脚手架

### Task 1: 创建 release/3.0 分支并删除旧前端

**Files:**
- Delete: `web/`
- Delete: `nginx/`
- Modify: `.gitignore`（移除 web 相关旧规则中不再需要的条目）

**Interfaces:**
- Produces: 干净的 `release/3.0` 分支，仅保留后端与基础设施。

- [ ] **Step 1: 确认当前在 release/2.0 并切出新分支**

```bash
git checkout release/2.0
git pull origin release/2.0
git checkout -b release/3.0
```

- [ ] **Step 2: 删除 web/ 和 nginx/ 目录**

```bash
rm -rf web nginx
```

- [ ] **Step 3: 检查 .gitignore 并移除已不相关条目**

查看 `.gitignore`，移除如 `nginx/` 等不再需要的条目，保留 `node_modules`、`.env`、`.output` 等通用规则。

- [ ] **Step 4: 提交清理**

```bash
git add -A
git commit -m "chore: 删除 web/ 和 nginx/，为 3.0 重建做准备

- 从 release/2.0 切出 release/3.0
- 移除整个旧前端与 nginx 配置
- 保留 api/、docker-compose、scripts、CI 等基础设施"
```

---

### Task 2: 使用 TanStack CLI 初始化 TanStack Start 项目

**Files:**
- Create: `web/`（由 TanStack CLI 生成后合并）
- Create: `docs/AGENTS.md`
- Modify: `.gitignore`（如需要）

**Interfaces:**
- Produces: 可运行的 TanStack Start 项目基础，与 TanStack Intent skills 集成。

- [ ] **Step 1: 创建临时目录并运行 TanStack CLI**

```bash
mkdir -p /tmp/tanstack-scaffold
cd /tmp/tanstack-scaffold
npx @tanstack/cli@latest create my-tanstack-app --agent
```

在交互提示中选择：
- **Stack**: React
- **Starter**: blank
- **Toolchain**: 保持默认 CLI 工具链

- [ ] **Step 2: 检查生成的项目结构**

```bash
cd /tmp/tanstack-scaffold/my-tanstack-app
find . -type f -not -path './node_modules/*' -not -path './.git/*' | sort
```

确认生成的关键文件：
- `package.json`（含 scripts、dependencies、devDependencies）
- `tsconfig.json`
- `vite.config.ts` 或等效配置
- `app.config.ts`
- `src/app/__root.tsx`
- `src/app/styles.css`
- `src/routes/index.tsx`
- `index.html`

- [ ] **Step 3: 将生成内容合并到 web/**

```bash
cd /Users/admin/Developer/homespace/mimo-blog
rm -rf web
mv /tmp/tanstack-scaffold/my-tanstack-app web
```

- [ ] **Step 4: 安装依赖**

```bash
cd web
pnpm install
```

- [ ] **Step 5: 运行 TanStack Intent 安装并列出 skills**

```bash
cd web
npx @tanstack/intent@latest install
npx @tanstack/intent@latest list
```

保存 `npx @tanstack/intent@latest list` 的输出，用于后续步骤和 AGENTS.md。

- [ ] **Step 6: 根据 Intent skills 的指引调整项目**

阅读已安装的 TanStack Intent skills 文档，按当前推荐模式调整：
- 路由组织方式
- SSR/CSR 边界处理
- 数据获取模式（TanStack Query 集成）
- 环境变量管理方式
- 部署配置

不要凭猜测修改，优先遵循 Intent skills 中给出的当前官方模式。

- [ ] **Step 7: 验证 dev server**

```bash
cd web
pnpm dev
```

浏览器访问 `http://localhost:3000`，确认无报错。

- [ ] **Step 8: 更新 docs/AGENTS.md**

在 `docs/AGENTS.md` 中追加以下记录：

```markdown
## TanStack Start 3.0 初始化记录

### CLI 命令

```bash
npx @tanstack/cli@latest create my-tanstack-app --agent
```

### 选项

- Stack: React
- Starter: blank
- Toolchain: default CLI toolchain

### TanStack Intent 命令

```bash
npx @tanstack/intent@latest install
npx @tanstack/intent@latest list
```

### 已安装 skills

- （粘贴 Step 5 的输出）

### 环境变量

- `VITE_API_URL`: 后端 API 地址，必填
- `VITE_SERVER_ORIGIN`: SSR 时服务端 origin
- `VITE_SITE_URL`: 站点公开 URL
- `VITE_GITHUB_TOKEN`: GitHub 集成令牌，可选
- `VITE_ENABLE_ANALYTICS`: 是否启用分析，可选

### 部署说明

- 生产构建: `pnpm build`
- 生产启动: `pnpm start`
- 使用 Node server preset（由 TanStack Start 默认配置）

### 关键架构决策

- 使用 TanStack Router 文件路由
- SSR 首屏渲染，浏览器专属组件 lazy + Suspense
- TanStack Query 管理服务端状态
- Zustand 管理客户端全局状态

### 已知注意事项

- 不要直接使用 `import.meta.env`，统一通过 `src/lib/env.ts` 校验
- 禁止原生 confirm/alert/prompt
- 单文件单组件，建议不超过 400 行
```

- [ ] **Step 9: 提交**

```bash
git add -A
git commit -m "chore(web): 使用 TanStack CLI 初始化项目

- npx @tanstack/cli@latest create my-tanstack-app --agent
- 选择 React + blank starter + 默认工具链
- 合并生成项目到 web/ 目录
- 安装并运行 TanStack Intent install/list
- 在 docs/AGENTS.md 记录初始化决策与环境变量"
```

---

### Task 3: 配置 Biome、Tailwind 与全局样式

**Files:**
- Create: `web/biome.json`
- Create: `web/src/app/styles.css`
- Create: `web/components.json`

**Interfaces:**
- Produces: 统一的代码风格、格式化、lint 规则和主题 CSS 变量。

- [ ] **Step 1: 创建 web/biome.json**

```json
{
  "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": false,
    "ignore": ["./node_modules", "./.output", "./routeTree.gen.ts"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "organizeImports": {
    "enabled": true
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": {
        "noConsoleLog": "warn"
      },
      "style": {
        "useImportType": "error"
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double",
      "semicolons": "always"
    }
  }
}
```

- [ ] **Step 2: 创建 web/src/app/styles.css**

```css
@import "tailwindcss";

@theme {
  --color-brand: oklch(0.55 0.2 260);
  --color-accent-brand: oklch(0.65 0.18 260);
  --color-background: oklch(1 0 0);
  --color-foreground: oklch(0.1 0 0);
  --color-muted: oklch(0.95 0 0);
  --color-muted-foreground: oklch(0.45 0 0);
  --color-border: oklch(0.9 0 0);
  --font-sans: "Geist Variable", ui-sans-serif, system-ui, sans-serif;
}

.dark {
  --color-background: oklch(0.1 0 0);
  --color-foreground: oklch(0.95 0 0);
  --color-muted: oklch(0.2 0 0);
  --color-muted-foreground: oklch(0.65 0 0);
  --color-border: oklch(0.25 0 0);
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
}
```

- [ ] **Step 3: 初始化 shadcn/ui**

```bash
cd web
pnpm dlx shadcn@latest init --yes --defaults
```

根据提示选择 base color 为 neutral 或 zinc，风格选择 new-york。

- [ ] **Step 4: 安装基础 shadcn/ui 组件**

```bash
cd web
pnpm dlx shadcn@latest add button card dialog alert-dialog dropdown-menu input label select separator sheet skeleton sonner switch table tabs textarea badge avatar checkbox form
```

- [ ] **Step 5: 运行 Biome 检查**

```bash
cd web
pnpm format
pnpm lint:fix
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "chore(web): 配置 Biome、Tailwind v4 与 shadcn/ui

- 添加 biome.json 与全局样式
- 初始化 shadcn/ui 并安装基础组件
- 运行 format 与 lint 修复"
```

---

## Phase 2: 全局基础设施

### Task 4: 环境变量与全局 API 客户端

**Files:**
- Create: `web/src/lib/env.ts`
- Create: `web/src/lib/api.ts`
- Create: `web/src/lib/utils.ts`

**Interfaces:**
- Produces: `env.VITE_API_URL`, `api` axios instance with interceptors.

- [ ] **Step 1: 创建 web/src/lib/env.ts**

```typescript
import { z } from "zod";

const envSchema = z.object({
  VITE_API_URL: z.string().url(),
  VITE_SERVER_ORIGIN: z.string().url().optional(),
  VITE_SITE_URL: z.string().url().optional(),
  VITE_GITHUB_TOKEN: z.string().optional(),
  VITE_ENABLE_ANALYTICS: z.enum(["true", "false"]).optional().default("false"),
});

export const env = envSchema.parse(import.meta.env);

export type Env = z.infer<typeof envSchema>;
```

- [ ] **Step 2: 创建 web/src/lib/utils.ts**

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

/**
 * 合并 Tailwind 类名
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

/**
 * 格式化日期为本地字符串
 */
export function formatDate(date: string | Date) {
  return new Date(date).toLocaleDateString("zh-CN", {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}
```

- [ ] **Step 3: 创建 web/src/lib/api.ts**

```typescript
import axios, { AxiosError, type AxiosRequestConfig } from "axios";
import { env } from "./env";

/**
 * 全局 axios 实例
 */
export const api = axios.create({
  baseURL: env.VITE_API_URL,
  headers: {
    "Content-Type": "application/json",
  },
});

let isRefreshing = false;
let refreshSubscribers: Array<(token: string) => void> = [];

function subscribeTokenRefresh(callback: (token: string) => void) {
  refreshSubscribers.push(callback);
}

function onTokenRefreshed(token: string) {
  refreshSubscribers.forEach((callback) => callback(token));
  refreshSubscribers = [];
}

api.interceptors.request.use((config) => {
  const token = localStorage.getItem("accessToken");
  if (token && config.headers) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

api.interceptors.response.use(
  (response) => response,
  async (error: AxiosError) => {
    const originalRequest = error.config as AxiosRequestConfig & { _retry?: boolean };

    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        return new Promise((resolve) => {
          subscribeTokenRefresh((token) => {
            if (originalRequest.headers) {
              originalRequest.headers.Authorization = `Bearer ${token}`;
            }
            resolve(api(originalRequest));
          });
        });
      }

      originalRequest._retry = true;
      isRefreshing = true;

      try {
        const refreshToken = localStorage.getItem("refreshToken");
        const response = await axios.post(`${env.VITE_API_URL}/auth/refresh`, {
          refreshToken,
        });
        const { accessToken } = response.data;
        localStorage.setItem("accessToken", accessToken);
        onTokenRefreshed(accessToken);
        if (originalRequest.headers) {
          originalRequest.headers.Authorization = `Bearer ${accessToken}`;
        }
        return api(originalRequest);
      } catch {
        localStorage.removeItem("accessToken");
        localStorage.removeItem("refreshToken");
        window.location.href = "/login";
        return Promise.reject(error);
      } finally {
        isRefreshing = false;
      }
    }

    return Promise.reject(error);
  },
);
```

- [ ] **Step 4: 添加测试**

Create `web/tests/lib/utils.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { cn, formatDate } from "@/lib/utils";

describe("utils", () => {
  it("cn merges classes", () => {
    expect(cn("a", "b")).toBe("a b");
  });

  it("formatDate formats date", () => {
    expect(formatDate("2026-06-18")).toContain("2026");
  });
});
```

- [ ] **Step 5: 运行测试**

```bash
cd web
pnpm test
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(web): 添加环境变量校验、全局 API 客户端与工具函数

- lib/env.ts 使用 Zod 校验环境变量
- lib/api.ts 配置 axios 实例、token 注入与刷新逻辑
- lib/utils.ts 提供 cn 与 formatDate
- 添加 utils 单元测试"
```

---

### Task 5: 全局 Provider 与根布局

**Files:**
- Create: `web/src/app/__root.tsx`
- Create: `web/src/app/client.tsx`
- Create: `web/src/app/ssr.tsx`
- Create: `web/src/components/providers/QueryProvider.tsx`
- Create: `web/src/components/providers/ThemeProvider.tsx`
- Create: `web/src/components/providers/ToastProvider.tsx`
- Create: `web/src/components/providers/AuthProvider.tsx`

**Interfaces:**
- Produces: 全局 Provider 链与根路由组件。

- [ ] **Step 1: 创建 QueryProvider**

```typescript
// web/src/components/providers/QueryProvider.tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
import { useState, type ReactNode } from "react";

interface QueryProviderProps {
  /** 子元素 */
  children: ReactNode;
}

/**
 * 提供 TanStack Query Client
 */
export function QueryProvider({ children }: QueryProviderProps) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            staleTime: 5 * 60 * 1000,
            refetchOnWindowFocus: false,
          },
        },
      }),
  );

  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  );
}
```

- [ ] **Step 2: 创建 ThemeProvider**

```typescript
// web/src/components/providers/ThemeProvider.tsx
import { ThemeProvider as NextThemesProvider } from "next-themes";
import { type ReactNode } from "react";

interface ThemeProviderProps {
  /** 子元素 */
  children: ReactNode;
}

/**
 * 主题 Provider，支持 light/dark/system
 */
export function ThemeProvider({ children }: ThemeProviderProps) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="system" enableSystem>
      {children}
    </NextThemesProvider>
  );
}
```

- [ ] **Step 3: 创建 ToastProvider**

```typescript
// web/src/components/providers/ToastProvider.tsx
import { Toaster } from "@/components/ui/sonner";
import { type ReactNode } from "react";

interface ToastProviderProps {
  /** 子元素 */
  children: ReactNode;
}

/**
 * 全局 Toast Provider
 */
export function ToastProvider({ children }: ToastProviderProps) {
  return (
    <>
      {children}
      <Toaster position="top-right" />
    </>
  );
}
```

- [ ] **Step 4: 创建 AuthProvider**

```typescript
// web/src/components/providers/AuthProvider.tsx
import { useAuthStore } from "@/features/auth/store";
import { type ReactNode, useEffect } from "react";

interface AuthProviderProps {
  /** 子元素 */
  children: ReactNode;
}

/**
 * 初始化认证状态，从持久化存储恢复
 */
export function AuthProvider({ children }: AuthProviderProps) {
  const initialize = useAuthStore((state) => state.initialize);

  useEffect(() => {
    initialize();
  }, [initialize]);

  return children;
}
```

- [ ] **Step 5: 创建根路由 __root.tsx**

```typescript
// web/src/app/__root.tsx
import { createRootRoute, Outlet } from "@tanstack/react-router";
import { AuthProvider } from "@/components/providers/AuthProvider";
import { QueryProvider } from "@/components/providers/QueryProvider";
import { ThemeProvider } from "@/components/providers/ThemeProvider";
import { ToastProvider } from "@/components/providers/ToastProvider";
import "./styles.css";

export const Route = createRootRoute({
  component: RootComponent,
});

function RootComponent() {
  return (
    <QueryProvider>
      <ThemeProvider>
        <AuthProvider>
          <ToastProvider>
            <Outlet />
          </ToastProvider>
        </AuthProvider>
      </ThemeProvider>
    </QueryProvider>
  );
}
```

- [ ] **Step 6: 创建 client.tsx 和 ssr.tsx**

```typescript
// web/src/app/client.tsx
import { createRouter } from "@tanstack/react-router";
import { routeTree } from "./routeTree.gen";

const router = createRouter({ routeTree });

function App() {
  return <router.RouterProvider />;
}

const rootElement = document.getElementById("root");
if (rootElement) {
  const root = createRoot(rootElement);
  root.render(<App />);
}
```

```typescript
// web/src/app/ssr.tsx
import { createRouter } from "@tanstack/react-router";
import { routeTree } from "./routeTree.gen";

export function createAppRouter() {
  return createRouter({ routeTree });
}
```

- [ ] **Step 7: 验证 dev server**

```bash
cd web
pnpm dev
```

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat(web): 添加全局 Provider 链与根路由

- QueryProvider、ThemeProvider、ToastProvider、AuthProvider
- 根路由 __root.tsx 组合全局 Provider
- client.tsx 与 ssr.tsx 入口"
```

---

### Task 6: 认证状态管理

**Files:**
- Create: `web/src/features/auth/store.ts`
- Create: `web/src/features/auth/types.ts`
- Create: `web/src/features/auth/api/keys.ts`
- Create: `web/src/features/auth/api/mutations.ts`
- Create: `web/src/features/auth/schemas.ts`

**Interfaces:**
- Produces: `useAuthStore`, auth types, login/register API, zod schemas.

- [ ] **Step 1: 创建 types.ts**

```typescript
// web/src/features/auth/types.ts

/**
 * 登录请求参数
 */
export interface LoginInput {
  /** 邮箱 */
  email: string;
  /** 密码 */
  password: string;
}

/**
 * 注册请求参数
 */
export interface RegisterInput {
  /** 用户名 */
  username: string;
  /** 邮箱 */
  email: string;
  /** 密码 */
  password: string;
}

/**
 * 认证用户信息
 */
export interface AuthUser {
  /** 用户 ID */
  id: string;
  /** 用户名 */
  username: string;
  /** 邮箱 */
  email: string;
  /** 头像 URL */
  avatar?: string;
  /** 角色 */
  role: string;
}

/**
 * 登录响应
 */
export interface LoginResponse {
  /** 访问令牌 */
  accessToken: string;
  /** 刷新令牌 */
  refreshToken: string;
  /** 用户信息 */
  user: AuthUser;
}
```

- [ ] **Step 2: 创建 schemas.ts**

```typescript
// web/src/features/auth/schemas.ts
import { z } from "zod";

/**
 * 登录表单校验
 */
export const loginSchema = z.object({
  email: z.string().email("请输入有效的邮箱"),
  password: z.string().min(6, "密码至少 6 位"),
});

/**
 * 注册表单校验
 */
export const registerSchema = z
  .object({
    username: z.string().min(2, "用户名至少 2 位"),
    email: z.string().email("请输入有效的邮箱"),
    password: z.string().min(6, "密码至少 6 位"),
    confirmPassword: z.string(),
  })
  .refine((data) => data.password === data.confirmPassword, {
    message: "两次输入的密码不一致",
    path: ["confirmPassword"],
  });

export type LoginSchema = z.infer<typeof loginSchema>;
export type RegisterSchema = z.infer<typeof registerSchema>;
```

- [ ] **Step 3: 创建 api/keys.ts**

```typescript
// web/src/features/auth/api/keys.ts

/**
 * 认证模块 query key 工厂
 */
export const authKeys = {
  me: ["auth", "me"] as const,
};
```

- [ ] **Step 4: 创建 api/mutations.ts**

```typescript
// web/src/features/auth/api/mutations.ts
import { api } from "@/lib/api";
import { type LoginInput, type LoginResponse, type RegisterInput } from "../types";

/**
 * 登录
 */
export async function login(data: LoginInput): Promise<LoginResponse> {
  const response = await api.post("/auth/login", data);
  return response.data;
}

/**
 * 注册
 */
export async function register(data: RegisterInput): Promise<LoginResponse> {
  const response = await api.post("/auth/register", data);
  return response.data;
}

/**
 * 获取当前用户信息
 */
export async function fetchMe(): Promise<LoginResponse["user"]> {
  const response = await api.get("/auth/me");
  return response.data;
}
```

- [ ] **Step 5: 创建 store.ts**

```typescript
// web/src/features/auth/store.ts
import { create } from "zustand";
import { persist } from "zustand/middleware";
import { type AuthUser } from "./types";

interface AuthState {
  /** 当前用户 */
  user: AuthUser | null;
  /** 访问令牌 */
  accessToken: string | null;
  /** 刷新令牌 */
  refreshToken: string | null;
  /** 是否已初始化 */
  isInitialized: boolean;
  /** 设置认证信息 */
  setAuth: (auth: { user: AuthUser; accessToken: string; refreshToken: string }) => void;
  /** 清除认证 */
  clearAuth: () => void;
  /** 从持久化恢复 */
  initialize: () => void;
}

/**
 * 认证状态管理
 */
export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      refreshToken: null,
      isInitialized: false,
      setAuth: (auth) => set(auth),
      clearAuth: () => set({ user: null, accessToken: null, refreshToken: null }),
      initialize: () => set({ isInitialized: true }),
    }),
    {
      name: "auth-storage",
      partialize: (state) => ({
        user: state.user,
        accessToken: state.accessToken,
        refreshToken: state.refreshToken,
      }),
    },
  ),
);
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(auth): 添加认证类型、schema、API 与状态管理

- auth/types.ts 定义用户与认证类型
- auth/schemas.ts 使用 Zod 校验登录注册表单
- auth/api/ 集中管理认证 API 与 query key
- auth/store.ts 使用 Zustand + persist 管理认证状态"
```

---

## Phase 3: 前台布局与页面

### Task 7: 前台公共布局

**Files:**
- Create: `web/src/routes/_public.tsx`
- Create: `web/src/components/shared/Header.tsx`
- Create: `web/src/components/shared/Footer.tsx`
- Create: `web/src/components/shared/ThemeToggle.tsx`

**Interfaces:**
- Produces: 前台布局组件，包含 Header、Footer、主题切换。

- [ ] **Step 1: 创建 Header.tsx**

```typescript
// web/src/components/shared/Header.tsx
import { Link } from "@tanstack/react-router";
import { ThemeToggle } from "./ThemeToggle";

/**
 * 前台顶部导航
 */
export function Header() {
  return (
    <header className="sticky top-0 z-50 border-b bg-background/80 backdrop-blur">
      <div className="container mx-auto flex h-16 items-center justify-between px-4">
        <Link to="/" className="text-xl font-bold">
          Mimo
        </Link>
        <nav className="flex items-center gap-6">
          <Link to="/blog">博客</Link>
          <Link to="/projects">项目</Link>
          <Link to="/music">音乐</Link>
          <Link to="/about">关于</Link>
          <ThemeToggle />
        </nav>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: 创建 Footer.tsx**

```typescript
// web/src/components/shared/Footer.tsx

/**
 * 前台页脚
 */
export function Footer() {
  return (
    <footer className="border-t py-8">
      <div className="container mx-auto px-4 text-center text-sm text-muted-foreground">
        © {new Date().getFullYear()} Mimo Blog. All rights reserved.
      </div>
    </footer>
  );
}
```

- [ ] **Step 3: 创建 ThemeToggle.tsx**

```typescript
// web/src/components/shared/ThemeToggle.tsx
import { Button } from "@/components/ui/button";
import { Moon, Sun } from "lucide-react";
import { useTheme } from "next-themes";

/**
 * 主题切换按钮
 */
export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      aria-label="切换主题"
    >
      {theme === "dark" ? <Sun className="h-5 w-5" /> : <Moon className="h-5 w-5" />}
    </Button>
  );
}
```

- [ ] **Step 4: 创建 _public.tsx**

```typescript
// web/src/routes/_public.tsx
import { createFileRoute, Outlet } from "@tanstack/react-router";
import { Footer } from "@/components/shared/Footer";
import { Header } from "@/components/shared/Header";

export const Route = createFileRoute("/_public")({
  component: PublicLayout,
});

/**
 * 前台公共布局
 */
function PublicLayout() {
  return (
    <div className="flex min-h-screen flex-col">
      <Header />
      <main className="flex-1">
        <Outlet />
      </main>
      <Footer />
    </div>
  );
}
```

- [ ] **Step 5: 验证前台布局**

浏览器访问 `http://localhost:3000`，确认 Header/Footer 渲染正常。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(web): 添加前台公共布局

- Header、Footer、ThemeToggle 共享组件
- _public.tsx 布局包含导航与主题切换"
```

---

### Task 8: 首页

**Files:**
- Create: `web/src/routes/_public.index.tsx`
- Create: `web/src/features/posts/components/HeroSection.tsx`
- Create: `web/src/features/posts/components/RecentPostsSection.tsx`
- Create: `web/src/features/posts/api/keys.ts`
- Create: `web/src/features/posts/api/queries.ts`
- Create: `web/src/features/posts/types.ts`

**Interfaces:**
- Produces: 首页路由、Hero 组件、最近文章列表。

- [ ] **Step 1: 创建 posts/types.ts**

```typescript
// web/src/features/posts/types.ts

/**
 * 文章摘要
 */
export interface PostSummary {
  /** 文章 ID */
  id: string;
  /** 标题 */
  title: string;
  /** 摘要 */
  excerpt: string;
  /** 缩略图 */
  cover?: string;
  /** 作者 */
  author: string;
  /** 发布时间 */
  publishedAt: string;
  /** 浏览量 */
  viewCount: number;
}

/**
 * 文章详情
 */
export interface PostDetail extends PostSummary {
  /** 正文 HTML */
  content: string;
  /** 标签 */
  tags: string[];
  /** Slug */
  slug: string;
}
```

- [ ] **Step 2: 创建 posts/api/keys.ts 和 queries.ts**

```typescript
// web/src/features/posts/api/keys.ts

/**
 * 文章模块 query key 工厂
 */
export const postKeys = {
  lists: () => ["posts", "list"] as const,
  list: (filters: Record<string, unknown>) => [...postKeys.lists(), filters] as const,
  details: () => ["posts", "detail"] as const,
  detail: (slug: string) => [...postKeys.details(), slug] as const,
};
```

```typescript
// web/src/features/posts/api/queries.ts
import { api } from "@/lib/api";
import { type PostDetail, type PostSummary } from "../types";

/**
 * 获取文章列表
 */
export async function fetchPosts(params?: {
  page?: number;
  limit?: number;
}): Promise<{ items: PostSummary[]; total: number }> {
  const response = await api.get("/posts", { params });
  return response.data;
}

/**
 * 根据 slug 获取文章详情
 */
export async function fetchPostBySlug(slug: string): Promise<PostDetail> {
  const response = await api.get(`/posts/${slug}`);
  return response.data;
}
```

- [ ] **Step 3: 创建 HeroSection.tsx**

```typescript
// web/src/features/posts/components/HeroSection.tsx

/**
 * 首页 Hero 区域
 */
export function HeroSection() {
  return (
    <section className="container mx-auto px-4 py-24 text-center">
      <h1 className="text-4xl font-bold tracking-tight sm:text-6xl">Mimo Blog</h1>
      <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
        记录技术、设计与生活的数字花园。
      </p>
    </section>
  );
}
```

- [ ] **Step 4: 创建 RecentPostsSection.tsx**

```typescript
// web/src/features/posts/components/RecentPostsSection.tsx
import { useQuery } from "@tanstack/react-query";
import { Link } from "@tanstack/react-router";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { postKeys } from "../api/keys";
import { fetchPosts } from "../api/queries";
import { formatDate } from "@/lib/utils";

/**
 * 最近文章列表
 */
export function RecentPostsSection() {
  const { data, isLoading } = useQuery({
    queryKey: postKeys.list({ page: 1, limit: 6 }),
    queryFn: () => fetchPosts({ page: 1, limit: 6 }),
  });

  if (isLoading) {
    return <div className="container mx-auto px-4 py-12">加载中...</div>;
  }

  return (
    <section className="container mx-auto px-4 py-12">
      <h2 className="mb-8 text-2xl font-bold">最近文章</h2>
      <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        {data?.items.map((post) => (
          <Card key={post.id}>
            <CardHeader>
              <CardTitle>
                <Link to="/blog/$slug" params={{ slug: post.id }}>
                  {post.title}
                </Link>
              </CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-sm text-muted-foreground">{formatDate(post.publishedAt)}</p>
              <p className="mt-2 line-clamp-3 text-muted-foreground">{post.excerpt}</p>
            </CardContent>
          </Card>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 5: 创建 _public.index.tsx**

```typescript
// web/src/routes/_public.index.tsx
import { createFileRoute } from "@tanstack/react-router";
import { HeroSection } from "@/features/posts/components/HeroSection";
import { RecentPostsSection } from "@/features/posts/components/RecentPostsSection";

export const Route = createFileRoute("/_public/")({
  component: HomePage,
});

/**
 * 首页
 */
function HomePage() {
  return (
    <>
      <HeroSection />
      <RecentPostsSection />
    </>
  );
}
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(posts): 添加首页与最近文章列表

- posts/types.ts 定义文章类型
- posts/api/ 添加 query key 工厂与查询函数
- HeroSection 与 RecentPostsSection 组件
- _public.index.tsx 首页路由"
```

---

### Task 9: 博客列表与详情页

**Files:**
- Create: `web/src/routes/_public.blog.index.tsx`
- Create: `web/src/routes/_public.blog.$slug.tsx`
- Create: `web/src/features/posts/components/PostCard.tsx`
- Create: `web/src/features/posts/components/PostContent.tsx`
- Create: `web/src/features/posts/components/TableOfContents.tsx`

**Interfaces:**
- Produces: `/blog` 列表页与 `/blog/:slug` 详情页。

- [ ] **Step 1: 创建 PostCard.tsx**

```typescript
// web/src/features/posts/components/PostCard.tsx
import { Link } from "@tanstack/react-router";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { formatDate } from "@/lib/utils";
import { type PostSummary } from "../types";

interface PostCardProps {
  /** 文章摘要 */
  post: PostSummary;
}

/**
 * 文章卡片
 */
export function PostCard({ post }: PostCardProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          <Link to="/blog/$slug" params={{ slug: post.id }}>
            {post.title}
          </Link>
        </CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">
          {formatDate(post.publishedAt)} · {post.viewCount} 次阅读
        </p>
        <p className="mt-2 line-clamp-3 text-muted-foreground">{post.excerpt}</p>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 2: 创建 _public.blog.index.tsx**

```typescript
// web/src/routes/_public.blog.index.tsx
import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { PostCard } from "@/features/posts/components/PostCard";
import { postKeys } from "@/features/posts/api/keys";
import { fetchPosts } from "@/features/posts/api/queries";

export const Route = createFileRoute("/_public/blog/")({
  component: BlogListPage,
});

/**
 * 博客列表页
 */
function BlogListPage() {
  const { data, isLoading } = useQuery({
    queryKey: postKeys.list({ page: 1, limit: 12 }),
    queryFn: () => fetchPosts({ page: 1, limit: 12 }),
  });

  return (
    <div className="container mx-auto px-4 py-12">
      <h1 className="mb-8 text-3xl font-bold">博客</h1>
      {isLoading ? (
        <p>加载中...</p>
      ) : (
        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {data?.items.map((post) => <PostCard key={post.id} post={post} />)}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 创建 PostContent.tsx 和 TableOfContents.tsx**

```typescript
// web/src/features/posts/components/PostContent.tsx
import { type PostDetail } from "../types";

interface PostContentProps {
  /** 文章详情 */
  post: PostDetail;
}

/**
 * 文章正文渲染
 */
export function PostContent({ post }: PostContentProps) {
  return (
    <article className="prose dark:prose-invert max-w-none">
      <h1>{post.title}</h1>
      <div dangerouslySetInnerHTML={{ __html: post.content }} />
    </article>
  );
}
```

```typescript
// web/src/features/posts/components/TableOfContents.tsx

/**
 * 目录导航
 */
export function TableOfContents() {
  return <aside className="hidden lg:block">目录占位</aside>;
}
```

- [ ] **Step 4: 创建 _public.blog.$slug.tsx**

```typescript
// web/src/routes/_public.blog.$slug.tsx
import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { PostContent } from "@/features/posts/components/PostContent";
import { TableOfContents } from "@/features/posts/components/TableOfContents";
import { postKeys } from "@/features/posts/api/keys";
import { fetchPostBySlug } from "@/features/posts/api/queries";

export const Route = createFileRoute("/_public/blog/$slug")({
  component: BlogDetailPage,
});

/**
 * 博客详情页
 */
function BlogDetailPage() {
  const { slug } = Route.useParams();
  const { data, isLoading } = useQuery({
    queryKey: postKeys.detail(slug),
    queryFn: () => fetchPostBySlug(slug),
  });

  if (isLoading) {
    return <div className="container mx-auto px-4 py-12">加载中...</div>;
  }

  if (!data) {
    return <div className="container mx-auto px-4 py-12">文章不存在</div>;
  }

  return (
    <div className="container mx-auto px-4 py-12">
      <div className="grid gap-8 lg:grid-cols-[1fr_280px]">
        <PostContent post={data} />
        <TableOfContents />
      </div>
    </div>
  );
}
```

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(posts): 添加博客列表与详情页

- PostCard 组件
- _public.blog.index.tsx 列表页
- PostContent 与 TableOfContents 组件
- _public.blog.$slug.tsx 详情页"
```

---

### Task 10: 关于页与项目页

**Files:**
- Create: `web/src/routes/_public.about.tsx`
- Create: `web/src/routes/_public.projects.index.tsx`
- Create: `web/src/features/projects/types.ts`
- Create: `web/src/features/projects/api/keys.ts`
- Create: `web/src/features/projects/api/queries.ts`
- Create: `web/src/features/projects/components/ProjectCard.tsx`

**Interfaces:**
- Produces: `/about` 与 `/projects` 页面。

- [ ] **Step 1: 创建 projects/types.ts**

```typescript
// web/src/features/projects/types.ts

/**
 * 项目信息
 */
export interface Project {
  /** 项目 ID */
  id: string;
  /** 项目名称 */
  name: string;
  /** 描述 */
  description: string;
  /** 封面图 */
  cover?: string;
  /** 项目链接 */
  url?: string;
  /** GitHub 链接 */
  githubUrl?: string;
  /** 标签 */
  tags: string[];
}
```

- [ ] **Step 2: 创建 projects/api/keys.ts 和 queries.ts**

```typescript
// web/src/features/projects/api/keys.ts

/**
 * 项目模块 query key 工厂
 */
export const projectKeys = {
  lists: () => ["projects", "list"] as const,
};
```

```typescript
// web/src/features/projects/api/queries.ts
import { api } from "@/lib/api";
import { type Project } from "../types";

/**
 * 获取项目列表
 */
export async function fetchProjects(): Promise<Project[]> {
  const response = await api.get("/projects");
  return response.data;
}
```

- [ ] **Step 3: 创建 ProjectCard.tsx**

```typescript
// web/src/features/projects/components/ProjectCard.tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { type Project } from "../types";

interface ProjectCardProps {
  /** 项目 */
  project: Project;
}

/**
 * 项目卡片
 */
export function ProjectCard({ project }: ProjectCardProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>{project.name}</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">{project.description}</p>
        <div className="mt-4 flex flex-wrap gap-2">
          {project.tags.map((tag) => (
            <Badge key={tag} variant="secondary">
              {tag}
            </Badge>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 4: 创建 _public.about.tsx**

```typescript
// web/src/routes/_public.about.tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/_public/about")({
  component: AboutPage,
});

/**
 * 关于页
 */
function AboutPage() {
  return (
    <div className="container mx-auto px-4 py-12">
      <h1 className="mb-6 text-3xl font-bold">关于</h1>
      <p className="text-muted-foreground">这里展示个人介绍、技术栈与 GitHub 贡献。</p>
    </div>
  );
}
```

- [ ] **Step 5: 创建 _public.projects.index.tsx**

```typescript
// web/src/routes/_public.projects.index.tsx
import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { ProjectCard } from "@/features/projects/components/ProjectCard";
import { projectKeys } from "@/features/projects/api/keys";
import { fetchProjects } from "@/features/projects/api/queries";

export const Route = createFileRoute("/_public/projects/")({
  component: ProjectsPage,
});

/**
 * 项目列表页
 */
function ProjectsPage() {
  const { data, isLoading } = useQuery({
    queryKey: projectKeys.lists(),
    queryFn: fetchProjects,
  });

  return (
    <div className="container mx-auto px-4 py-12">
      <h1 className="mb-8 text-3xl font-bold">项目</h1>
      {isLoading ? (
        <p>加载中...</p>
      ) : (
        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {data?.map((project) => <ProjectCard key={project.id} project={project} />)}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(web): 添加关于页与项目列表页

- projects/types.ts、api/keys.ts、api/queries.ts
- ProjectCard 组件
- _public.about.tsx 与 _public.projects.index.tsx"
```

---

### Task 11: 认证页面

**Files:**
- Create: `web/src/routes/_auth.tsx`
- Create: `web/src/routes/_auth.login.tsx`
- Create: `web/src/routes/_auth.register.tsx`
- Create: `web/src/features/auth/components/LoginForm.tsx`
- Create: `web/src/features/auth/components/RegisterForm.tsx`

**Interfaces:**
- Produces: 登录/注册页面与表单组件。

- [ ] **Step 1: 创建 _auth.tsx**

```typescript
// web/src/routes/_auth.tsx
import { createFileRoute, Outlet } from "@tanstack/react-router";

export const Route = createFileRoute("/_auth")({
  component: AuthLayout,
});

/**
 * 认证页布局，无 Header/Footer
 */
function AuthLayout() {
  return (
    <div className="flex min-h-screen items-center justify-center px-4">
      <Outlet />
    </div>
  );
}
```

- [ ] **Step 2: 创建 LoginForm.tsx**

```typescript
// web/src/features/auth/components/LoginForm.tsx
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { loginSchema, type LoginSchema } from "../schemas";
import { useLoginMutation } from "../hooks/useLoginMutation";

/**
 * 登录表单
 */
export function LoginForm() {
  const { mutate, isPending } = useLoginMutation();
  const form = useForm<LoginSchema>({
    resolver: zodResolver(loginSchema),
  });

  return (
    <form onSubmit={form.handleSubmit((data) => mutate(data))} className="space-y-4">
      <div>
        <Label htmlFor="email">邮箱</Label>
        <Input id="email" type="email" {...form.register("email")} />
        {form.formState.errors.email && (
          <p className="text-sm text-destructive">{form.formState.errors.email.message}</p>
        )}
      </div>
      <div>
        <Label htmlFor="password">密码</Label>
        <Input id="password" type="password" {...form.register("password")} />
        {form.formState.errors.password && (
          <p className="text-sm text-destructive">{form.formState.errors.password.message}</p>
        )}
      </div>
      <Button type="submit" disabled={isPending} className="w-full">
        {isPending ? "登录中..." : "登录"}
      </Button>
    </form>
  );
}
```

- [ ] **Step 3: 创建 useLoginMutation hook**

```typescript
// web/src/features/auth/hooks/useLoginMutation.ts
import { useMutation } from "@tanstack/react-query";
import { useNavigate } from "@tanstack/react-router";
import { toast } from "sonner";
import { login } from "../api/mutations";
import { useAuthStore } from "../store";
import { type LoginInput } from "../types";

/**
 * 登录 mutation
 */
export function useLoginMutation() {
  const navigate = useNavigate();
  const setAuth = useAuthStore((state) => state.setAuth);

  return useMutation({
    mutationFn: login,
    onSuccess: (data) => {
      setAuth({
        user: data.user,
        accessToken: data.accessToken,
        refreshToken: data.refreshToken,
      });
      toast.success("登录成功");
      navigate({ to: "/" });
    },
    onError: () => {
      toast.error("登录失败，请检查邮箱和密码");
    },
  });
}
```

- [ ] **Step 4: 创建 _auth.login.tsx**

```typescript
// web/src/routes/_auth.login.tsx
import { createFileRoute, Link } from "@tanstack/react-router";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { LoginForm } from "@/features/auth/components/LoginForm";

export const Route = createFileRoute("/_auth/login")({
  component: LoginPage,
});

/**
 * 登录页
 */
function LoginPage() {
  return (
    <Card className="w-full max-w-md">
      <CardHeader>
        <CardTitle>登录</CardTitle>
      </CardHeader>
      <CardContent>
        <LoginForm />
        <p className="mt-4 text-center text-sm">
          还没有账号？<Link to="/register">注册</Link>
        </p>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 5: 类似实现 RegisterForm 和 _auth.register.tsx**

创建 `RegisterForm.tsx` 与 `_auth.register.tsx`，使用 `registerSchema` 与 `useRegisterMutation`。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(auth): 添加登录与注册页面

- _auth.tsx 认证布局
- LoginForm、RegisterForm 组件
- useLoginMutation、useRegisterMutation hooks
- _auth.login.tsx 与 _auth.register.tsx 路由"
```

---

## Phase 4: 后台管理

### Task 12: 后台布局与权限守卫

**Files:**
- Create: `web/src/routes/admin.tsx`
- Create: `web/src/features/admin-layout/components/AdminSidebar.tsx`
- Create: `web/src/features/admin-layout/components/AdminHeader.tsx`
- Create: `web/src/features/admin-layout/hooks/useAdminNav.ts`

**Interfaces:**
- Produces: `/admin` 布局与 `admin:access` 权限守卫。

- [ ] **Step 1: 创建 admin.tsx**

```typescript
// web/src/routes/admin.tsx
import { createFileRoute, Outlet, redirect } from "@tanstack/react-router";
import { AdminHeader } from "@/features/admin-layout/components/AdminHeader";
import { AdminSidebar } from "@/features/admin-layout/components/AdminSidebar";
import { useAuthStore } from "@/features/auth/store";

export const Route = createFileRoute("/admin")({
  beforeLoad: () => {
    const { user } = useAuthStore.getState();
    if (!user?.role || user.role === "user") {
      throw redirect({ to: "/login" });
    }
  },
  component: AdminLayout,
});

/**
 * 后台布局
 */
function AdminLayout() {
  return (
    <div className="flex min-h-screen flex-col">
      <AdminHeader />
      <div className="flex flex-1">
        <AdminSidebar />
        <main className="flex-1 p-6">
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 useAdminNav.ts**

```typescript
// web/src/features/admin-layout/hooks/useAdminNav.ts

export interface AdminNavItem {
  /** 导航标题 */
  title: string;
  /** 路由路径 */
  to: string;
  /** 权限码 */
  permission?: string;
}

/**
 * 后台导航配置
 */
export function useAdminNav(): AdminNavItem[] {
  return [
    { title: "仪表盘", to: "/admin" },
    { title: "文章", to: "/admin/posts" },
    { title: "用户", to: "/admin/users" },
    { title: "评论", to: "/admin/comments" },
    { title: "媒体", to: "/admin/media" },
    { title: "设置", to: "/admin/settings" },
    { title: "角色权限", to: "/admin/roles" },
    { title: "公告", to: "/admin/announcements" },
    { title: "表情", to: "/admin/emojis" },
    { title: "歌单", to: "/admin/playlists" },
    { title: "日志", to: "/admin/logs" },
  ];
}
```

- [ ] **Step 3: 创建 AdminSidebar.tsx**

```typescript
// web/src/features/admin-layout/components/AdminSidebar.tsx
import { Link } from "@tanstack/react-router";
import { useAdminNav } from "../hooks/useAdminNav";

/**
 * 后台侧边栏
 */
export function AdminSidebar() {
  const navItems = useAdminNav();

  return (
    <aside className="hidden w-64 border-r bg-muted/30 lg:block">
      <nav className="space-y-1 p-4">
        {navItems.map((item) => (
          <Link
            key={item.to}
            to={item.to}
            className="block rounded-md px-3 py-2 text-sm hover:bg-muted"
            activeProps={{ className: "bg-muted font-medium" }}
          >
            {item.title}
          </Link>
        ))}
      </nav>
    </aside>
  );
}
```

- [ ] **Step 4: 创建 AdminHeader.tsx**

```typescript
// web/src/features/admin-layout/components/AdminHeader.tsx

/**
 * 后台顶部栏
 */
export function AdminHeader() {
  return (
    <header className="border-b bg-background px-6 py-3">
      <div className="font-semibold">Mimo Admin</div>
    </header>
  );
}
```

- [ ] **Step 5: 创建 admin/index.tsx**

```typescript
// web/src/routes/admin/index.tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/admin/")({
  component: AdminDashboardPage,
});

/**
 * 后台仪表盘
 */
function AdminDashboardPage() {
  return <div className="text-2xl font-bold">仪表盘</div>;
}
```

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(admin): 添加后台布局与权限守卫

- admin.tsx 布局与 beforeLoad 权限检查
- AdminHeader、AdminSidebar 组件
- useAdminNav 导航配置
- admin/index.tsx 仪表盘占位"
```

---

### Task 13: 后台文章管理

**Files:**
- Create: `web/src/routes/admin/posts.index.tsx`
- Create: `web/src/routes/admin/posts.new.tsx`
- Create: `web/src/routes/admin/posts.$id.edit.tsx`
- Create: `web/src/features/admin-posts/types.ts`
- Create: `web/src/features/admin-posts/schemas.ts`
- Create: `web/src/features/admin-posts/api/keys.ts`
- Create: `web/src/features/admin-posts/api/queries.ts`
- Create: `web/src/features/admin-posts/api/mutations.ts`
- Create: `web/src/features/admin-posts/components/PostTable.tsx`
- Create: `web/src/features/admin-posts/components/PostForm.tsx`

**Interfaces:**
- Produces: `/admin/posts` 列表、新建、编辑页面。

- [ ] **Step 1: 创建 admin-posts 类型与 schema**

```typescript
// web/src/features/admin-posts/types.ts
import { type PostDetail } from "@/features/posts/types";

/**
 * 后台文章项
 */
export interface AdminPost extends PostDetail {
  /** 状态 */
  status: PostStatus;
}

/**
 * 文章状态
 */
export type PostStatus = "draft" | "published" | "archived";
```

```typescript
// web/src/features/admin-posts/schemas.ts
import { z } from "zod";

/**
 * 文章表单校验
 */
export const postFormSchema = z.object({
  title: z.string().min(1, "标题不能为空"),
  slug: z.string().min(1, "Slug 不能为空"),
  excerpt: z.string().max(300, "摘要最多 300 字"),
  content: z.string().min(1, "正文不能为空"),
  cover: z.string().url().optional(),
  status: z.enum(["draft", "published", "archived"]),
  tags: z.array(z.string()),
});

export type PostFormSchema = z.infer<typeof postFormSchema>;
```

- [ ] **Step 2: 创建 admin-posts API 层**

```typescript
// web/src/features/admin-posts/api/keys.ts

export const adminPostKeys = {
  lists: () => ["admin", "posts", "list"] as const,
  detail: (id: string) => ["admin", "posts", "detail", id] as const,
};
```

```typescript
// web/src/features/admin-posts/api/queries.ts
import { api } from "@/lib/api";
import { type AdminPost } from "../types";

export async function fetchAdminPosts(): Promise<AdminPost[]> {
  const response = await api.get("/admin/posts");
  return response.data;
}

export async function fetchAdminPost(id: string): Promise<AdminPost> {
  const response = await api.get(`/admin/posts/${id}`);
  return response.data;
}
```

```typescript
// web/src/features/admin-posts/api/mutations.ts
import { api } from "@/lib/api";
import { type PostFormSchema } from "../schemas";

export async function createPost(data: PostFormSchema) {
  const response = await api.post("/admin/posts", data);
  return response.data;
}

export async function updatePost(id: string, data: PostFormSchema) {
  const response = await api.put(`/admin/posts/${id}`, data);
  return response.data;
}

export async function deletePost(id: string) {
  await api.delete(`/admin/posts/${id}`);
}
```

- [ ] **Step 3: 创建 PostTable.tsx**

```typescript
// web/src/features/admin-posts/components/PostTable.tsx
import { useQuery } from "@tanstack/react-query";
import { Link } from "@tanstack/react-router";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import { adminPostKeys } from "../api/keys";
import { fetchAdminPosts } from "../api/queries";

/**
 * 后台文章列表表格
 */
export function PostTable() {
  const { data, isLoading } = useQuery({
    queryKey: adminPostKeys.lists(),
    queryFn: fetchAdminPosts,
  });

  if (isLoading) return <p>加载中...</p>;

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>标题</TableHead>
          <TableHead>状态</TableHead>
          <TableHead>操作</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {data?.map((post) => (
          <TableRow key={post.id}>
            <TableCell>{post.title}</TableCell>
            <TableCell>{post.status}</TableCell>
            <TableCell>
              <Link to="/admin/posts/$id/edit" params={{ id: post.id }}>
                编辑
              </Link>
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}
```

- [ ] **Step 4: 创建 admin/posts.index.tsx**

```typescript
// web/src/routes/admin/posts.index.tsx
import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { PostTable } from "@/features/admin-posts/components/PostTable";

export const Route = createFileRoute("/admin/posts/")({
  component: AdminPostsPage,
});

function AdminPostsPage() {
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">文章管理</h1>
        <Button asChild>
          <Link to="/admin/posts/new">新建文章</Link>
        </Button>
      </div>
      <PostTable />
    </div>
  );
}
```

- [ ] **Step 5: 类似实现新建与编辑页**

创建 `PostForm.tsx` 与 `posts.new.tsx`、`posts.$id.edit.tsx`，使用 React Hook Form + Zod + Tiptap 编辑器（客户端渲染）。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat(admin-posts): 添加后台文章管理

- admin-posts 类型、schema、API 层
- PostTable 列表组件
- 新建/编辑文章页面与表单"
```

---

### Task 14: 后台其他管理模块

**Files:**
- Create: `web/src/routes/admin/users.tsx`
- Create: `web/src/routes/admin/comments.tsx`
- Create: `web/src/routes/admin/media.tsx`
- Create: `web/src/routes/admin/settings.tsx`
- Create: `web/src/routes/admin/roles.tsx`
- Create: `web/src/routes/admin/announcements.tsx`
- Create: `web/src/routes/admin/emojis.tsx`
- Create: `web/src/routes/admin/playlists.tsx`
- Create: `web/src/routes/admin/logs.tsx`
- Create: 对应 feature 目录

**Interfaces:**
- Produces: 所有后台管理页面与对应 API 层。

- [ ] **Step 1: 为每个后台模块创建 feature 目录结构**

为以下模块创建 `features/admin-*` 目录：
- admin-users
- admin-comments
- admin-media
- admin-settings
- admin-roles
- admin-announcements
- admin-emojis
- admin-playlists
- admin-logs

每个目录包含 `types.ts`、`schemas.ts`、`api/keys.ts`、`api/queries.ts`、`api/mutations.ts`、`components/`。

- [ ] **Step 2: 为每个模块创建路由占位页面**

每个 `web/src/routes/admin/*.tsx` 至少实现：

```typescript
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/admin/users")({
  component: AdminUsersPage,
});

function AdminUsersPage() {
  return <div className="text-2xl font-bold">用户管理</div>;
}
```

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "feat(admin): 添加后台各管理模块占位与目录结构

- 为 users/comments/media/settings/roles/announcements/emojis/playlists/logs 创建 feature 目录
- 添加对应路由占位页面"
```

---

## Phase 5: 复杂模块与高级功能

### Task 15: 音乐播放器

**Files:**
- Create: `web/src/routes/_public.music.tsx`
- Create: `web/src/features/music/types.ts`
- Create: `web/src/features/music/api/keys.ts`
- Create: `web/src/features/music/api/queries.ts`
- Create: `web/src/features/music/components/MusicPlayer.tsx`

**Interfaces:**
- Produces: 音乐播放器页面，客户端懒加载。

- [ ] **Step 1: 创建 music types 与 API**

```typescript
// web/src/features/music/types.ts

/**
 * 歌曲
 */
export interface Song {
  /** 歌曲 ID */
  id: string;
  /** 标题 */
  title: string;
  /** 艺术家 */
  artist: string;
  /** 音频 URL */
  url: string;
  /** 封面 */
  cover?: string;
}

/**
 * 歌单
 */
export interface Playlist {
  /** 歌单 ID */
  id: string;
  /** 名称 */
  name: string;
  /** 歌曲列表 */
  songs: Song[];
}
```

```typescript
// web/src/features/music/api/queries.ts
import { api } from "@/lib/api";
import { type Playlist } from "../types";

export async function fetchActivePlaylist(): Promise<Playlist> {
  const response = await api.get("/music/playlists/active");
  return response.data;
}
```

- [ ] **Step 2: 创建 MusicPlayer.tsx（客户端组件）**

```typescript
// web/src/features/music/components/MusicPlayer.tsx
import { lazy, Suspense } from "react";
import { useQuery } from "@tanstack/react-query";
import { musicKeys } from "../api/keys";
import { fetchActivePlaylist } from "../api/queries";

const APlayerComponent = lazy(() => import("./APlayerComponent"));

/**
 * 音乐播放器入口，客户端懒加载
 */
export function MusicPlayer() {
  const { data } = useQuery({
    queryKey: musicKeys.active(),
    queryFn: fetchActivePlaylist,
  });

  if (!data) return <p>暂无歌单</p>;

  return (
    <Suspense fallback={<p>播放器加载中...</p>}>
      <APlayerComponent playlist={data} />
    </Suspense>
  );
}
```

- [ ] **Step 3: 创建 _public.music.tsx**

```typescript
// web/src/routes/_public.music.tsx
import { createFileRoute } from "@tanstack/react-router";
import { MusicPlayer } from "@/features/music/components/MusicPlayer";

export const Route = createFileRoute("/_public/music")({
  component: MusicPage,
});

function MusicPage() {
  return (
    <div className="container mx-auto px-4 py-12">
      <h1 className="mb-8 text-3xl font-bold">音乐</h1>
      <MusicPlayer />
    </div>
  );
}
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat(music): 添加音乐播放器页面

- music types 与 API
- MusicPlayer 组件客户端懒加载
- _public.music.tsx 页面"
```

---

### Task 16: 富文本编辑器

**Files:**
- Create: `web/src/components/editor/RichTextEditor.tsx`
- Create: `web/src/components/editor/EditorToolbar.tsx`

**Interfaces:**
- Produces: 可在文章表单中使用的 Tiptap 编辑器。

- [ ] **Step 1: 创建 RichTextEditor.tsx**

```typescript
// web/src/components/editor/RichTextEditor.tsx
import { useEditor, EditorContent } from "@tiptap/react";
import StarterKit from "@tiptap/starter-kit";
import Placeholder from "@tiptap/extension-placeholder";
import { EditorToolbar } from "./EditorToolbar";

interface RichTextEditorProps {
  /** 编辑器内容 */
  content: string;
  /** 内容变化回调 */
  onChange: (content: string) => void;
}

/**
 * Tiptap 富文本编辑器
 */
export function RichTextEditor({ content, onChange }: RichTextEditorProps) {
  const editor = useEditor({
    extensions: [
      StarterKit,
      Placeholder.configure({ placeholder: "开始写作..." }),
    ],
    content,
    onUpdate: ({ editor }) => {
      onChange(editor.getHTML());
    },
  });

  if (!editor) return null;

  return (
    <div className="rounded-md border">
      <EditorToolbar editor={editor} />
      <EditorContent editor={editor} className="prose max-w-none p-4" />
    </div>
  );
}
```

- [ ] **Step 2: 创建 EditorToolbar.tsx**

```typescript
// web/src/components/editor/EditorToolbar.tsx
import { type Editor } from "@tiptap/react";
import { Button } from "@/components/ui/button";

interface EditorToolbarProps {
  /** Tiptap editor 实例 */
  editor: Editor;
}

/**
 * 编辑器工具栏
 */
export function EditorToolbar({ editor }: EditorToolbarProps) {
  return (
    <div className="flex gap-1 border-b p-2">
      <Button
        type="button"
        variant={editor.isActive("bold") ? "default" : "ghost"}
        size="sm"
        onClick={() => editor.chain().focus().toggleBold().run()}
      >
        粗体
      </Button>
      <Button
        type="button"
        variant={editor.isActive("italic") ? "default" : "ghost"}
        size="sm"
        onClick={() => editor.chain().focus().toggleItalic().run()}
      >
        斜体
      </Button>
    </div>
  );
}
```

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "feat(editor): 添加 Tiptap 富文本编辑器

- RichTextEditor 组件
- EditorToolbar 工具栏
- 在文章表单中集成编辑器"
```

---

### Task 17: ReactBits 视觉特效

**Files:**
- Create: `web/src/components/reactbits/AuroraBackground.tsx`
- Create: `web/src/components/reactbits/GradientText.tsx`
- Modify: `web/src/features/posts/components/HeroSection.tsx`

**Interfaces:**
- Produces: ReactBits 动画组件封装。

- [ ] **Step 1: 安装 reactbits 相关依赖**

ReactBits 组件通常通过 copy-paste 方式集成到项目中。将需要的动画组件源码放入 `components/reactbits/`。

- [ ] **Step 2: 创建 AuroraBackground.tsx**

```typescript
// web/src/components/reactbits/AuroraBackground.tsx
import { cn } from "@/lib/utils";

interface AuroraBackgroundProps {
  /** 子元素 */
  children: React.ReactNode;
  /** 额外类名 */
  className?: string;
}

/**
 * ReactBits Aurora 背景效果
 */
export function AuroraBackground({ children, className }: AuroraBackgroundProps) {
  return (
    <div className={cn("relative overflow-hidden", className)}>
      <div className="pointer-events-none absolute inset-0 opacity-30">
        <div className="absolute -left-[50%] -top-[50%] h-[200%] w-[200%] animate-aurora bg-gradient-to-r from-brand via-accent-brand to-transparent" />
      </div>
      <div className="relative z-10">{children}</div>
    </div>
  );
}
```

- [ ] **Step 3: 在首页 Hero 中使用**

修改 `HeroSection.tsx`：

```typescript
import { AuroraBackground } from "@/components/reactbits/AuroraBackground";

export function HeroSection() {
  return (
    <AuroraBackground className="py-24">
      <div className="container mx-auto px-4 text-center">
        <h1 className="text-4xl font-bold tracking-tight sm:text-6xl">Mimo Blog</h1>
      </div>
    </AuroraBackground>
  );
}
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat(reactbits): 集成 ReactBits 视觉特效

- 添加 AuroraBackground 等 ReactBits 组件封装
- 在首页 Hero 中应用 Aurora 背景"
```

---

## Phase 6: 测试、CI 与部署

### Task 18: 测试覆盖

**Files:**
- Create: `web/tests/setup.ts`
- Create: `web/tests/features/auth/store.test.ts`
- Create: `web/tests/features/posts/api.test.ts`

**Interfaces:**
- Produces: Vitest 测试基础设施与示例测试。

- [ ] **Step 1: 创建 tests/setup.ts**

```typescript
// web/tests/setup.ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 2: 创建 store.test.ts**

```typescript
// web/tests/features/auth/store.test.ts
import { describe, expect, it } from "vitest";
import { useAuthStore } from "@/features/auth/store";

describe("auth store", () => {
  it("sets auth state", () => {
    const { setAuth } = useAuthStore.getState();
    setAuth({
      user: { id: "1", username: "test", email: "test@example.com", role: "user" },
      accessToken: "token",
      refreshToken: "refresh",
    });
    expect(useAuthStore.getState().user?.username).toBe("test");
  });
});
```

- [ ] **Step 3: 运行测试**

```bash
cd web
pnpm test
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "test(web): 添加 Vitest 测试基础设施

- tests/setup.ts 配置
- auth store 与 posts api 示例测试"
```

---

### Task 19: CI 与 Docker 配置

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `docker-compose.yml`
- Modify: `docker-compose.prod.yml`

**Interfaces:**
- Produces: 更新后的 CI 与 Docker 配置，适配新的 web/ 项目。

- [ ] **Step 1: 更新 CI workflow**

确保 CI 中的 web job 运行：

```yaml
- name: Install dependencies
  run: pnpm install
  working-directory: ./web

- name: Type check
  run: pnpm typecheck
  working-directory: ./web

- name: Lint
  run: pnpm lint
  working-directory: ./web

- name: Test
  run: pnpm test
  working-directory: ./web

- name: Build
  run: pnpm build
  working-directory: ./web
```

- [ ] **Step 2: 验证 Docker Compose 中的 web 服务**

确保 `docker-compose.yml` 和 `docker-compose.prod.yml` 中的 web 服务使用新的构建上下文和启动命令。

- [ ] **Step 3: 本地验证构建**

```bash
cd web
pnpm build
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "chore(ci): 更新 CI 与 Docker 配置适配 3.0

- CI 中 web job 使用 pnpm typecheck/lint/test/build
- 验证 Docker Compose web 服务配置
- 本地构建通过"
```

---

### Task 20: 最终验证与收尾

**Files:**
- Modify: `README.md`（如有需要）
- Modify: `docs/2.0-roadmap.md`（更新为 3.0 状态）

**Interfaces:**
- Produces: 完整的 release/3.0 分支，可构建、可运行。

- [ ] **Step 1: 运行完整质量检查**

```bash
cd web
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

- [ ] **Step 2: 检查 git 状态**

```bash
git status
```

- [ ] **Step 3: 提交最终调整**

```bash
git add -A
git commit -m "chore(release): 完成 3.0 前端重构基础框架

- 所有前台页面、后台管理、认证、音乐播放器、编辑器、ReactBits 特效
- Biome、TypeScript、Vitest、CI、Docker 配置全部验证通过"
```

---

## Self-Review

### Spec Coverage

| Spec Section | Implementing Task |
|---|---|
| 分支策略：release/2.0 切出 release/3.0 | Task 1 |
| 删除 web/ 和 nginx/ | Task 1 |
| 技术栈：TanStack Start 等 | Task 2 |
| 代码规范：单文件单组件、400 行、Zod 表单 | Tasks 3-19 |
| 目录结构 | Tasks 2-14 |
| 架构分层 | Tasks 5, 7, 12 |
| 数据流 | Tasks 4, 6 |
| SSR/CSR 边界 | Tasks 15, 16 |
| 路由规划 | Tasks 7-12, 14 |
| 后台管理 | Tasks 12-14 |
| 复杂模块 | Tasks 15-17 |
| 测试与质量 | Tasks 4, 18, 19 |

### Placeholder Scan

- 无 "TBD"、"TODO"、"implement later"。
- 所有代码步骤均包含示例代码。
- 每个任务都以可验证的命令或提交结束。

### Type Consistency

- `PostSummary` / `PostDetail` 在 posts 与 admin-posts feature 之间共享。
- `AuthUser` 在 auth store 与 login mutation 中一致使用。
- Query key 工厂命名统一为 `featureKeys.xxx()`。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-18-mimo-blog-3.0.md`. Two execution options:

1. **Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
